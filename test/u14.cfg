# Unit test configuration file for MessageLogger service:
# test message suppression by severity and module

process TEST = {
  include "FWCore/Framework/test/cmsExceptionsFatal.cff"
  service = MessageLogger {
    
    untracked
    vstring destinations = { "u14_warnings"
			   , "u14_errors"
			   , "u14_infos"
			   , "u14_debugs"
			   , "u14_default"
			   }

    untracked 
    vstring fwkJobReports = { "u14_job_report.mxml" }

    untracked 
    vstring debugModules = { "*" }

    untracked 
    vstring suppressInfo = { "sendSomeMessages" }

    untracked 
    vstring categories = { "preEventProcessing" }

    untracked 
    PSet u14_errors   = { untracked bool noTimeStamps = true      
                              untracked string threshold  = "ERROR"   
			    }
			      
    untracked 
    PSet u14_warnings = { untracked bool noTimeStamps = true  
                              untracked string threshold  = "WARNING" 
			    }
			      
    untracked 
    PSet u14_infos    = { untracked bool noTimeStamps = true  
                              untracked string threshold  = "INFO"    
			      untracked PSet preEventProcessing = { untracked int32 limit    = 0 }
			    }  
			    
    untracked 
    PSet u14_debugs   = {untracked  bool noTimeStamps = true   
                              untracked string threshold  = "DEBUG"   
			      untracked PSet preEventProcessing = { untracked int32 limit    = 0 }
			    }
				
    untracked 
    PSet u14_default  = { untracked bool noTimeStamps = true         
			      untracked PSet preEventProcessing = { untracked int32 limit    = 0 }
			    }
  }

  include "FWCore/Services/data/InitRootHandlers.cfi"
  
  path p = { sendSomeMessages }

  module sendSomeMessages = UnitTestClient_I { }

  source = EmptySource {untracked int32 maxEvents = 2}
}
