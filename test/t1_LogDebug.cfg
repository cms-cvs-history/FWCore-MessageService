# Problem Report Investigation responding to Sunanda Banerjee:
# Trying to use LogDebug in Geometry/TrackerCommonData - no success in getting
# the debug printout with a straightforward configuration file:
#
# Use this with t1_LogDebg.cc

process TEST = {
  include "FWCore/Framework/test/cmsExceptionsFatal.cff"
  untracked PSet maxEvents = {untracked int32 input = 2}
  service = MessageLogger {
    untracked vstring destinations = {"warning","debugmessages"}
    untracked vstring categories = {  "TrackerGeom"}
    untracked vstring debugModules = { "*" }
    untracked PSet warning	   = {string threshold = "WARNING"}
    untracked PSet debugmessages = {string threshold = "DEBUG"
			    PSet INFO	   = { int32 limit = 0 }
			    PSet DEBUG	   = { int32 limit = 0 }
			    PSet TrackerGeom = { int32 limit = 1000000 }
			    }
  }

  include "FWCore/Services/data/InitRootHandlers.cfi"

#If instead I use edm::LogDebug_ rather than the macro the same code and cfg
#file gives the printout. Is there some copilation flag to be used somewhere in
#building  the libraries?                                                                                

  path p = { sendSomeMessages }

  module sendSomeMessages = ProblemTestClient_t1 { }

  source = EmptySource { }
}
