# Unit test configuration file for MessageLogger service:
# use of extensionless filenames

process TEST = {
  include "FWCore/Framework/test/cmsExceptionsFatal.cff"
  untracked PSet maxEvents = {untracked int32 input = 1}
  service = MessageLogger {
    
    untracked
    vstring destinations = { "u10_warnings" }

    untracked
    vstring fwkJobReports = { "u10_job_report" }
			      
    untracked
    PSet u10_warnings = { untracked bool noTimeStamps = true  
                          untracked string threshold  = "WARNING" 
			}
			      
  }

  include "FWCore/MessageService/test/Services.cff"
  
  path p = { sendSomeMessages }

  module sendSomeMessages = UnitTestClient_C { }

  source = EmptySource { }
}
